## 2026-02-18

**Done**
- Added `ExchangeRate` model to Prisma schema (`prisma/schema.prisma`)
- Applied schema change to Supabase database using `prisma db push`
- Seeded initial USD exchange rate: 1 USD = 3,660.25 COP (Banco de la República market average)
- Prisma Client regenerated with new model

**Decisions** (see DECISIONS.md)
- [SHARED] Added ExchangeRate model for USD price conversion on nouvie-web
- Used `prisma db push` again (consistent with existing pattern, no migration history)

**Blocked/Waiting**
- nouvie-web needs updated `schema.prisma` copied over + `npx prisma generate`
- nouvie-web needs `getExchangeRate()` function (Step 2 of USD implementation)
- nouvie-web needs UI changes to display USD prices

**Learned**
- Migration drift still exists — `prisma migrate dev` wants to reset the entire DB
- `prisma db push` remains the safe approach for this project until migration history is bootstrapped
- `prisma db execute` requires `--schema prisma/schema.prisma` flag to locate the datasource

**Next**
- In nouvie-web: copy updated schema, run `prisma generate`
- In nouvie-web: implement `getExchangeRate()` utility function
- In nouvie-web: add USD price display to product pages
- Consider bootstrapping migration history with `prisma migrate resolve` to fix drift long-term
