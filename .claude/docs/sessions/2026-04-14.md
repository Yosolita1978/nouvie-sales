# Session: 2026-04-14

## Done
- Added `month` query parameter to `GET /api/orders` — filters orders by YYYY-MM format
- Created monthly orders view at `/orders/month/[month]` with:
  - Month name header in Spanish with prev/next navigation arrows
  - Same filters as main page (status, payment method, search)
  - Cobrado/Por Cobrar stats for the month
  - Direct "Descargar Mes" export button (no modal)
  - Back link to all orders
- Created `GET /api/orders/months` endpoint — returns distinct months that have orders with counts
- Added "Ver pedidos por mes" navigation section to main `/orders` page — only shows months with data, with order count per month
- Redesigned month navigation to avoid confusion with filters (gray container, calendar icon, arrow links)

## Decisions
- Monthly route uses `/orders/month/[month]` (not `/orders/[month]`) to avoid conflicts with `/orders/[id]`
- `month` param takes priority over `period` param in the API (else-if)
- Month navigation only shows months that have orders (dynamic from DB), not a fixed last-N-months list
- Monthly export uses existing `/api/orders/export` with calculated from/to dates rather than a new endpoint

## Blocked
- Nothing

## Learned
- The "Abril 2026" highlighted button in the month nav looked like an active filter, causing confusion about whether all orders or just April were displayed — removed active state styling

## Next
- Consider extracting shared order card and filter components to reduce duplication between `/orders/page.tsx` and `/orders/month/[month]/page.tsx`
